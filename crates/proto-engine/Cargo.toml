[package]
name = "proto-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
nilestream-ledger = { path = "../nilestream-ledger" }

[dev-dependencies]
# The reference oracle, as a *test* dependency: the engine must never be able to
# reach the definition of correctness it is checked against.
conservation-suite = { path = "../conservation-suite" }
