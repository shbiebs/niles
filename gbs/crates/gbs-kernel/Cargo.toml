[package]
name = "gbs-kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

# The kernel sits directly on the ledger and on nothing else. Every dependency added here
# is a decision to make the general core less general, so the list is meant to stay short.
[dependencies]
nilestream-ledger = { path = "../../../crates/nilestream-ledger" }
