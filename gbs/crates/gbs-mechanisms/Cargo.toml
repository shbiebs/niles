[package]
name = "gbs-mechanisms"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

# M2..M7. Depends on the kernel and on nothing below it: a mechanism is a *pattern* of
# posting sets, so it needs M1 and no engine access at all. `tests/layering.rs` checks this.
[dependencies]
gbs-kernel = { path = "../gbs-kernel" }
